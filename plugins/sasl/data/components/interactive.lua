-------------------------------------------------------------------------------
-- Interactive zone
-------------------------------------------------------------------------------

function draw()
    if globalShowInteractiveAreas then
        sasl.gl.drawFrame(0, 0, size[1], size[2])
    end
end

-------------------------------------------------------------------------------
-------------------------------------------------------------------------------
